projection;
strict ( 1 );
use draft;

define behavior for ZC_STOCKAGEUPLOAD //alias <alias_name>
use etag
{
  //use create;
  use update;
  use delete;

  use action processLogic;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}