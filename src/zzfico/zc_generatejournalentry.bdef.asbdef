projection;
strict;
use draft;

define behavior for ZC_GenerateJournalEntry //alias <alias_name>
use etag

{
  use create;
  use update;
  use delete;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}